import {NgModule} from '@angular/core';
import {HttpGrupoContableService} from './http-grupo-contable-service';

@NgModule({
  providers: [HttpGrupoContableService],
})
export class HttpGrupoContableModule {
}
