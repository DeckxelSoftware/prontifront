import {NgModule} from '@angular/core';
import {HttpClienteEnGrupoService} from './http-cliente-en-grupo-service';

@NgModule({
  providers: [HttpClienteEnGrupoService],
})
export class HttpClienteEnGrupoModule {
}
